{
  "name": "exec-briefing-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.6",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
